version: "3.9"

services:
  app:
    build:
      context: .
      target: app
      args:
        # Prod’da API adresi
        VITE_API_URL: "https://muhasebe.berdx.com"
    environment:
      - PORT=3001
    ports:
      - "8050:3001"
